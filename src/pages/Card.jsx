import React, { useEffect, useState } from 'react'
import axios from 'axios';

const Card = () => {
    // const [allCards, setAllCards] = useState([]);
    // const code = localStorage.getItem("Oauth");
    // useEffect(async () => {
    //     const cardResponse = await fetch(`http://localhost:8080/api/getAllCards/${code}`, {
    //         method: "GET",
    //     })

    // }, [allCards]);


    return (
        <div>Card</div>
    )
}

export default Card